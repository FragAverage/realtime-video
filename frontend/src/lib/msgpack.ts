/**
 * This file is no longer used.
 * The backend uses plain JSON for config messages
 * and raw binary JPEG for frame data — no msgpack needed.
 *
 * Kept for reference in case we need binary serialization later.
 */
